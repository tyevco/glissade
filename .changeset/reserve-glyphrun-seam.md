---
"@glissade/scene": patch
---

docs: reserve a comment-only `glyphRun` op seam in the `DrawCommand` union (§3 text shaping) for a future harfbuzzjs shaper, deferred to post-1.0. No type or runtime surface is added.
