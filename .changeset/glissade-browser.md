---
'@glissade/browser': minor
---

New `@glissade/browser` package: a single-file IIFE bundle of the realtime embed path (core+scene+canvas2d+player+element) for `<script src>` / no-build browser use; `window.glissade.*`, auto-registers `<gs-player>`.
