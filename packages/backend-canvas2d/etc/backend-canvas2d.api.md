## API Report File for "@glissade/backend-canvas2d"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { BackendCaps } from '@glissade/scene';
import { DisplayList } from '@glissade/scene';
import { DrawCommand } from '@glissade/scene';
import { FontSpec } from '@glissade/scene';
import { RenderBackend } from '@glissade/scene';
import { ShaderCaps } from '@glissade/scene';
import { ShaderRef } from '@glissade/scene';
import { TextMetricsLite } from '@glissade/scene';
import { VideoFrameSource } from '@glissade/scene';

// @public (undocumented)
export class Canvas2DBackend implements RenderBackend {
    // Warning: (ae-forgotten-export) The symbol "AnyCanvas" needs to be exported by the entry point index.d.ts
    constructor(target: AnyCanvas, opts?: {
        shaderCaps?: ShaderCaps;
    });
    get canvas(): AnyCanvas;
    get caps(): BackendCaps;
    // (undocumented)
    dispose(): void;
    // (undocumented)
    measureText(text: string, font: FontSpec): TextMetricsLite;
    // (undocumented)
    readPixels(): Promise<Uint8ClampedArray>;
    // (undocumented)
    render(list: DisplayList): void;
    // Warning: (ae-forgotten-export) The symbol "Drawable" needs to be exported by the entry point index.d.ts
    setImageAsset(assetId: string, image: Drawable): void;
    setVideoAsset(assetId: string, source: VideoFrameSource): void;
}

export { DisplayList }

export { DrawCommand }

// @public (undocumented)
export function setShaderRunner(runner: ShaderRunner | null): void;

// @public
export interface ShaderRunner {
    // (undocumented)
    apply(layer: AnyCanvas, shader: ShaderRef, w: number, h: number): Drawable | null;
}

export { TextMetricsLite }

// (No @packageDocumentation comment for this package)

```
