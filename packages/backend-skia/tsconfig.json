{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"]
  },
  "include": ["src", "test"],
  // parity.test.ts is a cross-package integration test (playwright + vite from
  // the workspace root, DOM globals via the harness page); vitest transpiles
  // and runs it, but it is outside this node-typed package's tsc scope.
  "exclude": ["test/parity.test.ts", "test/export-web.test.ts"]
}
